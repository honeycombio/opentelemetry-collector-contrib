type: dedupe

status:
  class: processor
  stability:
    development: [logs]
  distributions: []
  warnings: []
  codeowners:
    active: [MikeGoldsmith, codeboten]

tests:
  config:
  goleak:
    ignore:
      top:
        - "github.com/hashicorp/golang-lru/v2/expirable.NewLRU[...].func1"

telemetry:
  metrics:
    dedupeprocessor_dropped:
      description: Number of dropped log records
      unit: "{records}"
      enabled: true
      sum:
        value_type: int
        monotonic: true
    dedupeprocessor_output:
      description: Number of data log records output
      unit: "{records}"
      enabled: true
      sum:
        value_type: int
        monotonic: true
